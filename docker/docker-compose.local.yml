services:
  postgres:
    # 공식 postgis/postgis는 arm64 이미지를 제공하지 않아 Apple Silicon에서 에뮬레이션으로 돈다.
    # imresamu/postgis는 공식 저장소의 빌드 스크립트로 만든 멀티아키(amd64·arm64) 이미지이며 태그 체계가 동일하다.
    # 태그는 운영(postgis/postgis:16-3.4, infra/terraform/variables.tf)과 같은 메이저로 맞춘다.
    # 두 이미지 모두 postgis 확장을 public에 설치해 spatial_ref_sys가 생긴다 — Flyway baseline이 필요한 이유(application.yml).
    image: imresamu/postgis:16-3.4
    container_name: tmt-postgres-local
    environment:
      - TZ=Asia/Seoul
      - POSTGRES_DB=tmt
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-12345678}
    command: postgres -c timezone=Asia/Seoul
    volumes:
      - postgres-local-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  postgres-local-data:
